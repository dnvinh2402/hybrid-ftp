# Build output
build/
build_*/
cmake-build-*/

# Platform/editor metadata
.DS_Store
__MACOSX/
.vscode/*.log

# Compiled binaries/packages
*.exe
*.deb

# Runtime/generated transfer data
*.tmp_ascii
server_files/
client_files/*.deb

# The server root is runtime data. The repository may still keep tracked
# sample files already committed under ftp_root/.
ftp_root/
